import { NavMenuComp } from '@components';

export const MainLayout = (props) => {
  const { children } = props;

  return (
    <div>
      {/*  <NavMenuComp />*/}
      sdf
      <>{children}</>
    </div>
  );
};
